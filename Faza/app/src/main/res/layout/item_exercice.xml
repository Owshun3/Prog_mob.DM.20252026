<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="12dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <EditText
        android:id="@+id/editNomExercice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nom exercice"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerSeries"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/btnSupprimerExercice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Supprimer exercice"
        android:textColor="@android:color/holo_red_dark"/>

    <Button
        android:id="@+id/btnAjouterSerie"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ajouter série"/>
</LinearLayout>
